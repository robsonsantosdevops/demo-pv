apiVersion: v1
kind: Secret
metadata:
  name: api-pv-secrets
  namespace: demo
type: Opaque
stringData:
  # Alinhar com db/postgres.yaml (POSTGRES_DB / POSTGRES_USER / POSTGRES_PASSWORD)
  # DNS curto funciona dentro do mesmo namespace (demo).
  db-host: postgres.demo.svc.cluster.local
  db-port: "5432"
  db-name: poliedro_db
  db-user: postgres
  db-password: postgres

